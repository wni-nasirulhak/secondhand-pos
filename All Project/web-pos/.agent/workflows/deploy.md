---
description: Deploy the application to Cloudflare Pages
---
1. Build the application for Cloudflare Pages
// turbo
```powershell
npm run pages:build
```
2. Verify the output in `.next` and `dist` (if applicable)
